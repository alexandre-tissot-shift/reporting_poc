# A dataset is a logical object that represents a set of related facts, attributes, and attribute labels.
# Datasets are basic organization units of a logical data model.
# Read more about Datasets:
# https://www.gooddata.com/developers/cloud-native/doc/cloud/model-data/concepts/dataset/

type: dataset
id: dim_claimcause

table_path: alexo_poc/dim_claimcause

title: Dim claimcause
description: Dim claimcause
tags:
  - Dim claimcause

primary_key: dim_claimcause.id

fields:
  claimcause:
    type: attribute
    source_column: claimcause
    data_type: STRING
    title: Claimcause
    description: Claimcause
    tags:
      - Dim claimcause
  dim_claimcause.id:
    type: attribute
    source_column: id
    data_type: INT
    title: ClaimCauseId
    description: ClaimCauseId
    tags:
      - Dim claimcause
