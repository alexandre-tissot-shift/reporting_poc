# A dataset is a logical object that represents a set of related facts, attributes, and attribute labels.
# Datasets are basic organization units of a logical data model.
# Read more about Datasets:
# https://www.gooddata.com/developers/cloud-native/doc/cloud/model-data/concepts/dataset/

type: dataset
id: dim_lob

table_path: alexo_poc/dim_lob

title: Dim lob
description: Dim lob
tags:
  - Dim lob

primary_key: dim_lob.id

fields:
  dim_lob.id:
    type: attribute
    source_column: id
    data_type: INT
    title: LineOfBusinessId
    description: LineOfBusinessId
    tags:
      - Dim lob
  lob:
    type: attribute
    source_column: lob
    data_type: STRING
    title: Lob
    description: Lob
    tags:
      - Dim lob
